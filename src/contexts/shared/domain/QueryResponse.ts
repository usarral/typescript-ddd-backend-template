/* eslint-disable @typescript-eslint/no-empty-object-type */
/**
 * Base interface for all query responses in the system.
 * This is a marker interface used for type safety.
 * Specific query responses should extend this interface with their own properties.
 */
export interface QueryResponse {}
